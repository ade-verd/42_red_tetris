import styled from 'styled-components';

const StyledActiveRooms = styled.div`
    .container {
        display: flex;
        flex-wrap: nowrap;
        flex-direction: row;
        justify-content: center;
        align-items: start;
        align-content: start;
        background: #111;
    }
`;

export default StyledActiveRooms;
